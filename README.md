# Teia Audit Chain

Notarização pública da audit chain do Teia GEO IGO.

Cada arquivo em `notarizations/` representa uma notarização diária — `merkle_root` + contagem de eventos + timestamp imutável.
